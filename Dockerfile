# Placeholder Dockerfile for v1 (code will be added later)
FROM node:22-alpine
WORKDIR /app

# In v1, we will copy package.json and source code here.
# For now, keep a placeholder to make docker-compose valid.
CMD ["node", "-e", "console.log('SJ AI News Discord Bot: placeholder image (no code yet).')"]
