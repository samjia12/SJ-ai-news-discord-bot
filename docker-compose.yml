services:
  app:
    image: sj-ai-news-discord-bot:dev
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - ./data:/data
    environment:
      - DB_PATH=/data/app.sqlite
    restart: unless-stopped
