# ===== Required =====
# Dashboard login
ADMIN_PASSWORD=change-me

# Discord
DISCORD_BOT_TOKEN=your-discord-bot-token

# Allowlist: comma-separated guild IDs (only these guilds can use the bot)
ALLOWLIST_GUILD_IDS=1437322703358136405

# Web dashboard port
PORT=3000

# SQLite path
# - In Docker, this should point to the mounted volume (default: /data/app.sqlite)
# - In local dev (npm run dev), use a relative path like ./data/app.sqlite
DB_PATH=./data/app.sqlite

# ===== Optional (v1 defaults; keep as-is) =====
RSS_URL=https://www.oaibest.com/rss.xml
POLL_INTERVAL_MINUTES=20
MAX_DAILY_ITEMS_PER_GUILD=300
MAX_OUTPUT_CHARS=700
